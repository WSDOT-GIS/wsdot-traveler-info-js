// Place your settings in this file to overwrite default and user settings.
{
  "files.exclude": {
    // Exclude complied output JavaScript and type definition files.
    // "**/{*Spec,CommonUtils,*Client,geoJsonUtils,support/*}.{js,d.ts}": { "when": "$(basename).ts" },
    ".tscache": true,
    // "**/*.js.map": true,
    "**/.git": true,
    "**/.svn": true,
    "**/.hg": true,
    "**/.DS_Store": true
  }
}